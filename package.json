{
  "name": "tvtime",
  "version": "1.0.0",
  "private": true,
  "description": "Suivi de séries et de films auto-hébergé, inspiré de TV Time.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "build": "npm run build --prefix web",
    "validate:collections": "node server/scripts/validate-collections.mjs",
    "dev": "concurrently -n api,web -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
